# Agent Configuration Guide

Codex uses global `AGENTS.md`; Claude Code uses global `CLAUDE.md`; Antigravity uses global `GEMINI.md`. Adapters import/reference the installed canonical invariants and Machine Profile. Never duplicate canonical project semantics in vendor adapters.
