# Project Configuration Guide

Bootstrap only router files plus `ENGINEERING_CONTRACT.md`. Preserve existing project files by marker integration. Project contract is the semantic source of truth; global machine safety remains inherited.
