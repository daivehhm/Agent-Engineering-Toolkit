# Operation and Maintenance

After Toolkit upgrade run self-test → `-WhatIf` → install/upgrade → refresh machine profile → static verify → fresh-session runtime smoke. Re-run smoke after vendor updates or adapter-path changes.
