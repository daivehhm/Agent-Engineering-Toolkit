# Troubleshooting

If static verification fails, repair markers/imports/version/skill drift before runtime testing. If runtime smoke fails while static checks pass, report `CONFIGURED_NOT_RUNTIME_VERIFIED`; do not claim SETUP_VERIFIED. If an Agent repeatedly violates a loaded rule, collect evidence before considering hard enforcement.
