# Calling Examples

Use `contract-impact-check` for semantic changes, `stage-execution` for stage work/formal acceptance, and `independent-review` for fresh review. Small local changes do not require ceremonial Stage machinery, but Global Safety always applies.
