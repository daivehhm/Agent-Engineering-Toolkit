# Migration / Upgrade Notes

For any existing AET installation, use `install-agent-engineering.ps1 -UpgradeCanonical -WhatIf`, inspect backups/planned changes, then run real upgrade. v1.6 adds FS_SEARCH_SAFETY_V1 and runtime behavioral verification without changing the three-Skill model.
