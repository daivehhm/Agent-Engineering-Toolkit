# Capability and Permission Prerequisites

Machine capability is not current-session permission. Before real work verify required project read/write, terminal, runtime, binaries, network/GPU/long-running access and human review access. Missing required capability => EXECUTION_BLOCKED.
