# Usage Scenarios and Decision Tree

Small change → direct scoped work. Semantic change → Contract Impact. Multi-file/runtime Stage → Stage Execution. Formal acceptance → frozen criteria + independent review. File search safety applies before all filesystem traversal regardless of work class.
