# Six-Layer Scope Model

L0 machine facts/local stricter policy; L1 cross-Agent invariants; L2 vendor adapters; L3 project semantics; L4 stage outcome contract; L5 independent review/human gate. Put a rule in the lowest layer that can own it without duplication.
