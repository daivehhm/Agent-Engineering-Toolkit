# Cross-Agent Execution and Review Protocol

Every supported Agent imports the same canonical invariants. Subagents and delegated tools inherit safety; adapters cannot bypass. Static configuration proof and fresh runtime proof are separate. Cross-agent review uses fresh context and treats Builder narrative as a claim, not evidence.
