# Evidence Contract
Observed evidence is immutable. Reports derive from canonical artifacts, not vice versa. Use explicit `UNPROVEN`/`NOT_RUN` where evidence is absent. Unit tests do not prove untested runtime paths.
