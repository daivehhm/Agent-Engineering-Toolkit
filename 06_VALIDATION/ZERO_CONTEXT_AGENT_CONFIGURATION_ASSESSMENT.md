# Zero-Context Agent Configuration Assessment — v1.6.1
A zero-context Agent must start at `00_START_HERE_FOR_ANY_AGENT.md`, use native Windows scripts, preserve unrelated config, verify static installation, then perform fresh-session runtime loading/behavior smoke. Absence of runtime proof => `CONFIGURED_NOT_RUNTIME_VERIFIED`.
